<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Dynamo.Controls"
    xmlns:models="clr-namespace:Dynamo.Models;assembly=DynamoCore"
    xmlns:nodes="clr-namespace:Dynamo.Nodes"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:ui="clr-namespace:Dynamo.UI"
    xmlns:viewModels="clr-namespace:Dynamo.ViewModels">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Dynamo.Wpf;component/Themes/Modern/DynamoConverters.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <DataTemplate DataType="{x:Type viewModels:NodeViewModel}">
        <controls:NodeView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type viewModels:NoteViewModel}">
        <nodes:NoteView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type viewModels:InfoBubbleViewModel}">
        <controls:InfoBubbleView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type viewModels:AnnotationViewModel}">
        <nodes:AnnotationView />
    </DataTemplate>

    <DataTemplate x:Key="InputPortView" DataType="{x:Type viewModels:InPortViewModel}">
        <controls:InPortView />
    </DataTemplate>

    <DataTemplate x:Key="IncomePortView" DataType="{x:Type viewModels:InPortViewModel}">
        <controls:IncomePortView />
    </DataTemplate>

    <DataTemplate x:Key="OutputPortView" DataType="{x:Type viewModels:OutPortViewModel}">
        <controls:OutPortView />
    </DataTemplate>
    <DataTemplate x:Key="OutcomePortView" DataType="{x:Type viewModels:OutPortViewModel}">
        <controls:OutcomePortView />
    </DataTemplate>
    <viewModels:PortDataTemplateSelector
        x:Key="PortDataTemplateSelector"
        IncomeDataTemplate="{StaticResource IncomePortView}"
        InputDataTemplate="{StaticResource InputPortView}"
        OutcomeDataTemplate="{StaticResource OutcomePortView}"
        OutputDataTemplate="{StaticResource OutputPortView}"
        />


</ResourceDictionary>
