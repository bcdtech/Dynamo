<Window
    x:Class="Dynamo.UI.Prompts.DynamoMessageBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fa5="http://schemas.fontawesome.com/icons/"
    xmlns:p="clr-namespace:Dynamo.Wpf.Properties"
    xmlns:ui="clr-namespace:Dynamo.UI"
    Title="{x:Static p:Resources.GenericTaskDialogTitle}"
    MinWidth="400"
    MaxWidth="500"
    AllowsTransparency="True"
    ResizeMode="NoResize"
    SizeToContent="WidthAndHeight"
    Style="{DynamicResource DynamoWindowStyle}"
    ToolTipService.IsEnabled="false"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None">
    <Window.Background>
        <SolidColorBrush Opacity="0" />
    </Window.Background>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>

                <ResourceDictionary Source="/Dynamo.Wpf;component/Themes/Modern/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="Close" Executed="OnCloseExecuted" />
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Key="Esc" Command="Close" />
    </Window.InputBindings>

    <Grid Background="Transparent" MouseDown="UIElement_OnMouseDown">

        <Border
            Name="MainBorder"
            Background="White"
            BorderThickness="4"
            CornerRadius="4 ">
            <Border.Effect>
                <DropShadowEffect
                    BlurRadius="10.0"
                    Opacity="0.25"
                    ShadowDepth="0"
                    Color="#000" />
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Title Bar + Close Button  -->
                <DockPanel
                    Grid.Row="0"
                    Margin="18,20"
                    HorizontalAlignment="Stretch">
                    <Image
                        Name="MessageBoxImageIcon"
                        Width="24"
                        Height="24"
                        Margin="0,0,5,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        DockPanel.Dock="Left">
                        <Image.Style>
                            <Style TargetType="Image">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=MessageBoxImage}" Value="{x:Static MessageBoxImage.Information}">
                                        <Setter Property="Source" Value="/Dynamo.Wpf;component/Assets/Images/info_2.png" />
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Path=MessageBoxImage}" Value="{x:Static MessageBoxImage.Question}">
                                        <Setter Property="Source" Value="/Dynamo.Wpf;component/Assets/Images/info_2.png" />
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Path=MessageBoxImage}" Value="{x:Static MessageBoxImage.Warning}">
                                        <Setter Property="Source" Value="/Dynamo.Wpf;component/Assets/Images/alert_2.png" />
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Path=MessageBoxImage}" Value="{x:Static MessageBoxImage.Exclamation}">
                                        <Setter Property="Source" Value="/Dynamo.Wpf;component/Assets/Images/alert_2.png" />
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Path=MessageBoxImage}" Value="{x:Static MessageBoxImage.Error}">
                                        <Setter Property="Source" Value="/Dynamo.Wpf;component/Assets/Images/error-icon.png" />
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>
                    <Button
                        Name="CloseButton"
                        Click="CloseButton_OnClick"
                        DockPanel.Dock="Right"
                        Style="{StaticResource CloseButtonStyle}" />
                    <TextBlock
                        Name="TitleTextBlock"
                        VerticalAlignment="Center"
                        DockPanel.Dock="Left"
                        FontFamily="{StaticResource ArtifaktElementRegular}"
                        FontSize="20px"
                        Foreground="#3C3C3C"
                        Text="{Binding TitleText, UpdateSourceTrigger=PropertyChanged}"
                        TextWrapping="Wrap" />
                    <Label
                        Name="TooltipInfo"
                        Width="53"
                        Height="26"
                        Margin="20,-3,0,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">
                        <Label.Style>
                            <Style TargetType="{x:Type Label}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ShowTooltip}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Label.Style>
                        <Label.ToolTip>
                            <ToolTip Content="{Binding Path=Tooltip}" Style="{StaticResource GenericToolTipLight}" />
                        </Label.ToolTip>
                        <Label.Content>
                            <fa5:ImageAwesome
                                Width="15"
                                Height="15"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Bottom"
                                Foreground="#808080"
                                Icon="Solid_QuestionCircle" />
                        </Label.Content>
                    </Label>
                </DockPanel>
                <Rectangle
                    Name="Separator"
                    Grid.Row="0"
                    Height="1px"
                    VerticalAlignment="Bottom"
                    Fill="#B7B7B7" />

                <!--  Body Text  -->
                <TextBlock
                    Name="BodyTextBlock"
                    Grid.Row="1"
                    Margin="20"
                    VerticalAlignment="Center"
                    DockPanel.Dock="Left"
                    FontFamily="{StaticResource ArtifaktElementRegular}"
                    FontSize="14px"
                    Foreground="#3C3C3C"
                    Text="{Binding BodyText, UpdateSourceTrigger=PropertyChanged}"
                    TextWrapping="Wrap" />
                <!--  Body text in scrollable text box  -->
                <ScrollViewer
                    Name="ScrollableBodyTextBlock"
                    Grid.Row="1"
                    MaxHeight="250"
                    Margin="20"
                    VerticalScrollBarVisibility="Auto"
                    Visibility="Collapsed">
                    <TextBlock
                        VerticalAlignment="Center"
                        DockPanel.Dock="Left"
                        FontFamily="{StaticResource ArtifaktElementRegular}"
                        FontSize="14px"
                        Foreground="#3C3C3C"
                        Text="{Binding BodyText, UpdateSourceTrigger=PropertyChanged}"
                        TextWrapping="Wrap" />
                </ScrollViewer>


                <!--  Buttons  -->
                <DockPanel
                    Name="ButtonsDockPanel"
                    Grid.Row="2"
                    Margin="16,0,20,20"
                    LastChildFill="False">

                    <Button
                        Name="YesButton"
                        MinWidth="90"
                        Margin="0"
                        Background="#0696D7"
                        BorderBrush="#0696D7"
                        Click="YesButton_OnClick"
                        Content="{x:Static p:Resources.GenericTaskDialogOptionYes}"
                        DockPanel.Dock="Right"
                        Foreground="White"
                        Style="{StaticResource CtaButtonStyle}"
                        Visibility="Collapsed" />

                    <Button
                        Name="OkButton"
                        MinWidth="90"
                        Margin="0"
                        Background="#0696D7"
                        BorderBrush="#0696D7"
                        Click="OkButton_OnClick"
                        Content="{x:Static p:Resources.GenericTaskDialogOptionOK}"
                        DockPanel.Dock="Right"
                        Foreground="White"
                        Style="{StaticResource CtaButtonStyle}"
                        Visibility="Collapsed" />

                    <Button
                        Name="NoButton"
                        MinWidth="90"
                        Margin="0"
                        Background="White"
                        BorderBrush="#BFBFBF"
                        Click="NoButton_OnClick"
                        Content="{x:Static p:Resources.GenericTaskDialogOptionNo}"
                        DockPanel.Dock="Right"
                        Foreground="Black"
                        Style="{StaticResource CtaButtonStyle}"
                        Visibility="Collapsed" />

                    <Button
                        Name="CancelButton"
                        MinWidth="90"
                        Margin="0"
                        Background="White"
                        BorderBrush="#BFBFBF"
                        Click="CancelButton_OnClick"
                        Content="{x:Static p:Resources.GenericTaskDialogOptionCancel}"
                        DockPanel.Dock="Left"
                        Foreground="Black"
                        Style="{StaticResource CtaButtonStyle}"
                        Visibility="Collapsed" />
                </DockPanel>
            </Grid>
        </Border>

        <!--  Status Flag  -->
        <Border
            Name="downloadStatusFlag"
            Width="4"
            Margin="0,4"
            HorizontalAlignment="Left"
            CornerRadius="4,0,0,4">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=MessageBoxImage}" Value="{x:Static MessageBoxImage.Information}">
                            <Setter Property="Background" Value="#0696D7" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=MessageBoxImage}" Value="{x:Static MessageBoxImage.Question}">
                            <Setter Property="Background" Value="#0696D7" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=MessageBoxImage}" Value="{x:Static MessageBoxImage.Warning}">
                            <Setter Property="Background" Value="{StaticResource YellowOrange500Brush}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=MessageBoxImage}" Value="{x:Static MessageBoxImage.Exclamation}">
                            <Setter Property="Background" Value="{StaticResource YellowOrange500Brush}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=MessageBoxImage}" Value="{x:Static MessageBoxImage.Stop}">
                            <Setter Property="Background" Value="Red" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>
    </Grid>
</Window>
