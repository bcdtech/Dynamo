<Window
    x:Class="Dynamo.UI.Prompts.GenericTaskDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:p="clr-namespace:Dynamo.Wpf.Properties"
    xmlns:ui="clr-namespace:Dynamo.UI"
    Title="{x:Static p:Resources.GenericTaskDialogTitle}"
    Width="660"
    AllowsTransparency="True"
    ResizeMode="NoResize"
    SizeToContent="Height"
    Style="{DynamicResource DynamoWindowStyle}"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None">

    <Window.Background>
        <SolidColorBrush Opacity="0" />
    </Window.Background>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Dynamo.Wpf;component/Themes/Modern/Generic.xaml" />

            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="Close" Executed="OnCloseExecuted" />
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Key="Esc" Command="Close" />
    </Window.InputBindings>

    <Grid Background="Transparent" MouseDown="UIElement_OnMouseDown">
        <Border Name="MainBorder" Style="{StaticResource NoStyleWindowBorderStyle}">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <!--  Title Bar + Close Button  -->
                <DockPanel
                    Grid.Row="0"
                    Grid.ColumnSpan="2"
                    Margin="18,20,18,35"
                    HorizontalAlignment="Stretch">
                    <Image
                        Name="MessageBoxImageIcon"
                        Width="24"
                        Height="24"
                        Margin="0,0,5,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        DockPanel.Dock="Left"
                        Source="/Dynamo.Wpf;component/UI/Images/dynamonotext.png" />

                    <Button
                        Name="CloseButton"
                        Margin="25,0,-5,0"
                        Click="CloseButton_OnClick"
                        DockPanel.Dock="Right"
                        Style="{StaticResource CloseButtonStyle}" />

                    <TextBlock
                        Name="TitleTextBlock"
                        VerticalAlignment="Center"
                        DockPanel.Dock="Left"
                        FontFamily="{StaticResource ArtifaktElementRegular}"
                        FontSize="20px"
                        Foreground="#3C3C3C"
                        Text="{x:Static p:Resources.GenericTaskDialogTitle}"
                        TextWrapping="Wrap" />
                </DockPanel>

                <Rectangle
                    Grid.Row="0"
                    Grid.ColumnSpan="2"
                    Style="{StaticResource DividerRectangleStyle}" />

                <Image
                    Name="DialogIcon"
                    Grid.Row="1"
                    Width="96"
                    Height="96"
                    Margin="25"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center" />

                <StackPanel
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="0,25,25,25">

                    <TextBlock
                        Name="SummaryText"
                        Margin="0,0,0,5"
                        HorizontalAlignment="Stretch"
                        FontSize="15"
                        Foreground="{StaticResource DarkThemeInputBoxBackgroundBrush}"
                        TextWrapping="Wrap" />

                    <TextBlock
                        Name="DescriptionText"
                        HorizontalAlignment="Stretch"
                        FontSize="12"
                        Foreground="{StaticResource DarkThemeInputBoxBackgroundBrush}"
                        TextWrapping="Wrap" />

                </StackPanel>

                <StackPanel
                    Name="LeftButtonStackPanel"
                    Grid.Row="2"
                    Grid.ColumnSpan="2"
                    Margin="10,0,10,10"
                    HorizontalAlignment="Left"
                    Orientation="Horizontal">

                    <Button
                        Background="White"
                        BorderBrush="#BFBFBF"
                        Content="{x:Static p:Resources.GenericTaskDialogSampleLeftButton}"
                        Foreground="#3C3C3C"
                        Style="{StaticResource CtaButtonStyle}"
                        Visibility="Visible" />

                </StackPanel>

                <StackPanel
                    Name="RightButtonStackPanel"
                    Grid.Row="2"
                    Grid.ColumnSpan="2"
                    Margin="10,0,10,10"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">

                    <Button
                        Background="#0696D7"
                        BorderBrush="#0696D7"
                        Content="{x:Static p:Resources.GenericTaskDialogSampleRightButton}"
                        Foreground="White"
                        Style="{StaticResource CtaButtonStyle}"
                        Visibility="Visible" />

                </StackPanel>

                <TextBox
                    Name="DetailedContent"
                    Grid.Row="4"
                    Grid.ColumnSpan="2"
                    Height="200"
                    Margin="10,0,10,10"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    x:FieldModifier="public"
                    BorderThickness="0"
                    FontSize="12"
                    Foreground="{StaticResource DarkThemeInputBoxBackgroundBrush}"
                    HorizontalScrollBarVisibility="Auto"
                    IsReadOnly="True"
                    Style="{DynamicResource ResourceKey=SLightTextBox}"
                    VerticalScrollBarVisibility="Auto"
                    Visibility="Visible" />
            </Grid>
        </Border>
    </Grid>
</Window>
