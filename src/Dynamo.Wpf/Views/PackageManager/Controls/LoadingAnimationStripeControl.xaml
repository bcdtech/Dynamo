<UserControl
    x:Class="Dynamo.PackageManager.UI.LoadingAnimationStripeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Dynamo.PackageManager.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    d:DesignHeight="200"
    d:DesignWidth="500"
    Background="Transparent"
    mc:Ignorable="d">
    <UserControl.Resources>
        <sys:Double x:Key="animationSpeed">1.5</sys:Double>
    </UserControl.Resources>
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <!--  Base color  -->
        <Rectangle
            Name="viewDetailsRectangleBackground"
            HorizontalAlignment="Stretch"
            Fill="#33808080" />
        <!--  Moving part  -->
        <Rectangle Name="viewDetailsRectangle" HorizontalAlignment="Stretch">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="1,0.5" EndPoint="0,0.5">
                    <GradientStop Offset="0" Color="#33808080" />
                    <GradientStop x:Name="GradiantStopA" Offset="0.5" Color="#808080" />
                    <GradientStop Offset="1" Color="#33808080" />
                </LinearGradientBrush>
            </Rectangle.Fill>
            <Rectangle.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <!--  Gradient offset animation  -->
                            <DoubleAnimationUsingKeyFrames
                                AccelerationRatio="0.5"
                                DecelerationRatio="0.5"
                                RepeatBehavior="Forever"
                                SpeedRatio="{DynamicResource animationSpeed}"
                                Storyboard.TargetName="GradiantStopA"
                                Storyboard.TargetProperty="Offset">
                                <LinearDoubleKeyFrame KeyTime="0:0:0" Value="0.9" />
                                <LinearDoubleKeyFrame KeyTime="0:0:1.5" Value="0.5" />
                                <LinearDoubleKeyFrame KeyTime="0:0:3" Value="0.1" />
                            </DoubleAnimationUsingKeyFrames>
                            <!--  Opacity animation  -->
                            <DoubleAnimationUsingKeyFrames
                                AccelerationRatio="0.5"
                                DecelerationRatio="0.5"
                                RepeatBehavior="Forever"
                                SpeedRatio="{DynamicResource animationSpeed}"
                                Storyboard.TargetProperty="Opacity">
                                <LinearDoubleKeyFrame KeyTime="0:0:0" Value="0" />
                                <LinearDoubleKeyFrame KeyTime="0:0:1.5" Value="1" />
                                <LinearDoubleKeyFrame KeyTime="0:0:3" Value="0" />
                                <LinearDoubleKeyFrame KeyTime="0:0:6" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Rectangle.Triggers>
        </Rectangle>
    </Grid>
</UserControl>
