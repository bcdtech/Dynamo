<UserControl
    x:Class="Dynamo.Wpf.Views.TrustedPathView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ViewModels="clr-namespace:Dynamo.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fa="http://schemas.fontawesome.io/icons/"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:p="clr-namespace:Dynamo.Wpf.Properties"
    xmlns:ui="clr-namespace:Dynamo.UI"
    d:DataContext="{d:DesignInstance ViewModels:TrustedPathViewModel,
                                     IsDesignTimeCreatable=False}"
    Background="#535353"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Dynamo.Wpf;component/UI/Themes/Modern/DynamoModern.xaml" />

            </ResourceDictionary.MergedDictionaries>
            <DataTemplate x:Key="trustedPathItemTemplate">

                <Grid>
                    <Grid.ToolTip>
                        <ToolTip Content="{Binding}" Style="{StaticResource GenericToolTipLight}" />
                    </Grid.ToolTip>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition MaxWidth="250" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!--  the path text  -->
                    <TextBlock
                        x:Name="PathTextBlock"
                        Grid.Column="0"
                        MinHeight="24"
                        Margin="0,4,7,4"
                        Padding="5,5,5,5"
                        Background="{StaticResource PreferencesWindowItemDarkerBackgroundColor}"
                        Foreground="{StaticResource PreferencesWindowFontColor}"
                        Style="{StaticResource DarkTextBlock}"
                        Text="{Binding}" />
                    <!--  the the edit path button  -->
                    <Button
                        x:Name="UpdatePathButton"
                        Grid.Column="1"
                        Margin="7,7,7,7"
                        Background="{StaticResource PreferencesWindowBackgroundColor}"
                        Command="{Binding Path=DataContext.UpdatePathCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                        CommandParameter="{Binding}"
                        Style="{StaticResource EditFolderButtonStyle}"
                        ToolTipService.ShowOnDisabled="True">
                        <Button.ToolTip>
                            <ToolTip Content="{x:Static p:Resources.PackagePathUpdatePathTooltip}" Style="{StaticResource GenericToolTipLight}" />
                        </Button.ToolTip>
                    </Button>
                    <!--  the delete path button  -->
                    <Button
                        x:Name="DeletePathButton"
                        Grid.Column="2"
                        Margin="7,7,7,7"
                        Background="{StaticResource PreferencesWindowBackgroundColor}"
                        Command="{Binding Path=DataContext.DeletePathCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                        CommandParameter="{Binding}"
                        Style="{StaticResource CloseButtonStyle}"
                        ToolTipService.ShowOnDisabled="True">
                        <Button.ToolTip>
                            <ToolTip Content="{x:Static p:Resources.PackagePathViewToolTipMinus}" Style="{StaticResource GenericToolTipLight}" />
                        </Button.ToolTip>
                    </Button>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel Margin="0,6,0,0" Orientation="Vertical">
        <Button
            x:Name="AddPaths"
            Margin="0"
            HorizontalAlignment="Left"
            Command="{Binding Path=AddPathCommand}"
            Content="{x:Static p:Resources.PackagePathAddPathButtonName}">
            <Button.ToolTip>
                <ToolTip Content="{x:Static p:Resources.PackagePathViewToolTipPlus}" Style="{StaticResource GenericToolTipLight}" />
            </Button.ToolTip>
            <Button.Style>
                <Style BasedOn="{StaticResource SolidButtonStyle}" TargetType="{x:Type Button}">
                    <Setter Property="IsEnabled" Value="True" />
                </Style>
            </Button.Style>
        </Button>
        <ItemsControl
            Margin="0,5,0,0"
            Background="{StaticResource PreferencesWindowVisualSettingsBackground}"
            ItemTemplate="{StaticResource trustedPathItemTemplate}"
            ItemsSource="{Binding TrustedLocations}" />
    </StackPanel>
</UserControl>

